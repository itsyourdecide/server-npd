# Карта портов switch1 (HP 3500yl-48G)

Версия: 1.1
Дата: 2026-07-02
Статус: фактическое текущее состояние одного физического свитча (в отличие от теоретической 2-switch/3-node таблицы в `cluster_lab_poc_plan.md`, раздел 6, которая описывает целевую схему после ввода `pve02`/`pve03` и второго свитча)

Этот документ — источник истины по факту "что физически воткнуто куда прямо сейчас". Обновлять при каждом изменении кабелей/портов, синхронно с записью в `cluster_operations_log.md`.

## Текущее назначение портов

| Порт | Устройство | Untagged (native) | Tagged | Статус |
|---:|---|---:|---|---|
| 1 | Общий лабораторный роутер | 99 (WAN_TEMP) | — | Up, 100FDx (роутер отдаёт только 100Mb — ограничение порта роутера) |
| 4 | `pve01 nic1` (транк) | 10 (MGMT) | 20, 30, 40, 50, 80, 99 | Up, 1000FDx |
| 14 | IPMI `pve01` | 30 (IPMI) | — | Up, 100FDx. Финальный порт по `production_port_plan.md` (диапазон 14-18 = IPMI `pve01`-`pve05`, `pve01` первый) |
| 2–3, 5–13, 15–48 | Свободны | (по умолчанию не в untagged-списке VLAN10 после пересборки — см. ниже) | — | Down |

Порт 26 — освобождён, IPMI `pve01` перенесён на порт 14 (2026-07-02, вечер).

## VLAN на свитче

| VLAN | Имя | Сеть | Untagged порты | Tagged порты |
|---:|---|---|---|---|
| 1 | DEFAULT_VLAN | — | нет (`no untagged 1-48`) | — |
| 10 | MGMT | `10.10.10.0/24` | 2-25, 27-48 (дефолт, всё кроме 1/4/26 — но реально используется только через порт 4) | — |
| 20 | COROSYNC | `10.10.20.0/24` | — | 4 |
| 30 | IPMI | `10.10.30.0/24` | 14 | 4 |
| 40 | PRIVATE_VM | `10.10.40.0/24` | — | 4 |
| 50 | DMZ | `10.10.50.0/24` | — | 4 |
| 80 | HTCONDOR_PXE | `10.10.80.0/24` | — | 4 |
| 99 | WAN_TEMP | DHCP лаборатории | 1 | 4 |

Свитч management IP: `10.10.10.2/24` (VLAN10), но реальный менеджмент идёт через serial-консоль (PuTTY), IP не используется для управления самим свитчом.

## Известные ограничения

- Порт 1 (роутер) согласовался только на **100Mb/s** — не проблема свитча/кабеля с нашей стороны, ограничение самого порта роутера (аналогичная ситуация ранее наблюдалась и на `nic0`/`vmbr0` до перехода на транк).
- Порт 4 (`nic1`) — честный гигабит, `1000FDx`.
- Второй свитч (switch2) пока не введён в эксплуатацию физически, теоретическая LACP-схема между двумя свитчами из `cluster_lab_poc_plan.md` не актуальна для текущего состояния.

## Что предстоит решить (открытые вопросы)

- Порты под `pve02`/`pve03` LAN1/LAN2 и IPMI — по `production_port_plan.md` уже известны (LAN1: порты 5-6, IPMI: порты 15-16), но физически не подключены, будут добавлены при вводе этих узлов.
- Судьба старого прямого кабеля роутер→`pve01 nic0` (сейчас всё ещё физически подключён как страховка, минуя свитч) — решение отложено до Фазы 5 плана перехода на VLAN.
