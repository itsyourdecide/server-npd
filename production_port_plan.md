# Целевой план распределения портов (production, 2× HP 3500yl-48G)

Версия: 1.1
Дата: 2026-07-02, статус ASUS-бюджета обновлён 2026-07-03
Статус: проект. Часть по Supermicro (раздел 3, порты 4-18 на switch1 и 3-17 на switch2) — рабочая, используется уже сейчас. **Часть по ASUS требует пересчёта** — инвентаризация первого узла (2026-07-03) показала, что условие из раздела ниже НЕ выполняется (см. раздел 5).
Связанные документы: `cluster_architecture_rationale.md` (обоснование), `cluster_lab_poc_plan.md` (лабораторный PoC, другая, временная схема), `switch1_port_map.md` (текущее фактическое состояние одного свитча в лаборатории)

## 1. Условие, при котором этот план работает

Бюджет портов сходится **только если** ASUS R21A поддерживают режим **Shared LAN / NC-SI** — BMC делит один физический порт с сетевым трафиком ОС узла.

> **Обновление 2026-07-03: условие НЕ выполняется.** Первый физически проверенный узел показал выделенный (dedicated) физический порт под IPMI, отдельный от LAN1/LAN2 — не shared. Детали и последствия для бюджета — раздел 5. Раскладка по портам в разделе 3 ниже (диапазоны 19-42 / 18-41 «LAN+IPMI совмещённо») **больше не актуальна как есть** и требует пересчёта под 2 порта на узел.

Если Shared LAN не поддерживается — бюджет НЕ сходится в рамках двух текущих HP 3500yl (нужно либо докупать 3-й свитч под IPMI, либо резать другие требования — Supermicro-резерв LAN или число одновременно подключённых ASUS). См. `cluster_architecture_rationale.md`, раздел 9, где этот риск уже был отмечен.

## 2. Бюджет портов

| Слой | Узлов | Портов/узел | Итого |
|---|---:|---|---:|
| Supermicro LAN1+LAN2 (резерв) | 10 | 2 | 20 |
| Supermicro IPMI (индивидуально) | 10 | 1 | 10 |
| ASUS LAN+IPMI (совмещённо, Shared LAN) | 48 | 1 | 48 |
| Аплинк (роутер/firewall) | — | — | 1 |
| LACP-транк между switch1/switch2 | — | — | 2 |
| **Итого занято** | | | **81** |
| Резерв на рост | | | **15** (из 96) |

## 3. Раскладка по портам

### Switch1 (48 портов)

| Порты | Назначение | Untagged | Tagged |
|---|---|---:|---|
| 1 | Аплинк (роутер/firewall) | WAN-сегмент | — |
| 2-3 | LACP-транк к switch2 | — | 10, 20, 40, 50, 80 |
| 4-13 | `pve01`-`pve10` LAN1 | 10 (native) | 20, 40, 50, 80 |
| 14-18 | `pve01`-`pve05` IPMI | 30 | — |
| 19-42 | ASUS-узлы #1-24, LAN+IPMI совмещённо | 80 (или отдельный тег IPMI, если BMC умеет тегировать сам) | 30 (если BMC поддерживает VLAN-тег для management-трафика) |
| 43-48 | Резерв | — | — |

### Switch2 (48 портов)

| Порты | Назначение | Untagged | Tagged |
|---|---|---:|---|
| 1-2 | LACP-транк к switch1 | — | 10, 20, 40, 50, 80 |
| 3-12 | `pve01`-`pve10` LAN2 | 10 (native) | 20, 40, 50, 80 |
| 13-17 | `pve06`-`pve10` IPMI | 30 | — |
| 18-41 | ASUS-узлы #25-48, LAN+IPMI совмещённо | 80 | 30 |
| 42-48 | Резерв | — | — |

**VLAN 60/61 (Ceph) намеренно нигде не тегируются на этих двух свитчах** — см. раздел 4.1: Ceph требует полностью отдельную физическую 10GbE-инфраструктуру, не 1GbE-трафик через HP 3500yl, даже под тегом.

## 4. Логика решений

- **Supermicro IPMI разделён 5/5 между свитчами** (не все 10 на одном) — чтобы отказ одного свитча не отрезал доступ к management сразу всех core/general-узлов, только к половине.
- **LAN1/LAN2 Supermicro на разных свитчах** — стандартная сетевая избыточность, отказ одного свитча не роняет узел полностью.
- **ASUS без резервного линка** — это динамический compute-слой без HA/quorum-требований (см. `cluster_architecture_rationale.md`, раздел 7), потеря сетевого доступа к одному ASUS-узлу уменьшает ёмкость, но не роняет инфраструктуру.
- **VLAN 99 (временный WAN лаборатории) в этом плане отсутствует** — он существует только в PoC (`cluster_lab_poc_plan.md`) и не переносится в production.

### 4.1 Почему Ceph (VLAN 60/61) не входит в раскладку этих двух свитчей

> **Статус 2026-07-02: Ceph отложен как основной путь к HA-хранилищу, см. `storage_decision_zfs_replication.md`.** Разделы 4.1-4.2 ниже сохранены как справочный расчёт — пригодятся при пересмотре решения (появление бюджета/б-у 10GbE), сейчас не выполняются.

Первая версия этого документа тегировала 60/61 на тех же LAN1/LAN2-портах Supermicro, что и остальной трафик — это ошибка. Даже с VLAN-изоляцией это физически один и тот же 1GbE-провод, разделяемый с management/DMZ/HTCondor-трафиком. По документации (`cluster_architecture_rationale.md`, 8.3) production Ceph требует **выделенную** сеть минимум на 10GbE — не просто отдельный VLAN на общем канале, а физически отдельная полоса пропускания, иначе Ceph recovery/backfill может помешать Corosync (риск потери quorum).

Если Ceph ставится хотя бы на несколько Supermicro (при "1 JBOD на ноду" — потенциально на все 10), нужна отдельная физическая инфраструктура под VLAN 60/61, вне этих двух HP 3500yl:

- **Вариант А** — отдельный небольшой 10GbE-свитч только под Ceph (для 10 узлов хватит 12-24-портового, не нужен полноразмерный 48-портовый).
- **Вариант Б** — 10GbE-модуль в HP 3500yl, если он там есть и портов достаточно (обычно 2-4 SFP+/X2 слота в модуле — вероятно недостаточно для 10 узлов одновременно, нужно проверять).

Открытый вопрос, блокирующий выбор: есть ли у остальных 9 Supermicro (кроме уже проверенного `pve01`) встроенные 10GbE-порты того же типа, что нашли у `pve01` (X10DRI-T, Intel X540) — если да, у нас уже есть аппаратная база для отдельной 10GbE Ceph-сети между узлами, не хватает только коммутации между ними.

### 4.2 Бюджетное решение на первую фазу (только 3 core-узла) — 2026-07-02

Бюджет на сетевое оборудование под Ceph ограничен **5000 грн (~$112)**. Проверены реальные цены на медные 10GBase-T свитчи: даже самый дешёвый новый вариант с достаточным числом портов — от **$699** (MikroTik CRS312-4C+8XG-RM, 12 медных портов), и это не зависит сильно от числа портов — даже 3-4-портовых дешёвых 10GBase-T моделей на рынке практически нет, рынок считает "доступным" от $240 и то за 8 портов. Ни один новый вариант не укладывается в бюджет.

**Решение для фазы "только pve01-03":** использовать **full mesh** — прямые кабели между тремя core-узлами без свитча вообще:

```text
pve01 nic0/nic1 -- прямой кабель -- pve02
pve02            -- прямой кабель -- pve03
pve01            -- прямой кабель -- pve03
```

Оба встроенных 10GbE-порта каждого из трёх core-узлов уходят под mesh (Ceph public+replication, возможно и Corosync). Так как mesh не даёт выхода наружу (только связь между этими тремя узлами), для обычного LAN/management-трафика на `pve01-03` нужна **третья** сетевая карта на узел — дешёвая PCIe 1GbE-карта (на порядок дешевле любого 10G-свитча, реалистично уложиться в бюджет на 3 карты).

Стоимость: **$0 за сетевую инфраструктуру Ceph** (только кабели) + 3× бюджетная 1GbE PCIe-карта под LAN.

Покупка полноценного 10GbE-свитча под все 10 Supermicro (Вариант А из 4.1) откладывается до расширения Ceph за пределы 3 core-узлов — тогда бюджет пересматривается отдельно, либо ищется б/у оборудование (у этого проекта уже есть прецедент работы со списанным железом).

**Требует проверки перед закупкой:**
- [ ] Есть ли свободные PCIe-слоты на `pve01`/`pve02`/`pve03` под третью сетевую карту (на `pve01` по инвентаризации 2026-07-01 видны свободные root ports, но не подтверждён физический слот).
- [ ] Поддержка full mesh routing в Proxmox (статическая маршрутизация или FRR/OpenFabric) — стандартная задокументированная схема, но требует настройки при внедрении.

## 5. Что нужно проверить перед тем, как считать план окончательным

- [x] **Поддержка Shared LAN/NC-SI на ASUS — проверено 2026-07-03, ответ: НЕТ, не shared.** На первом физически проверенном узле (шасси-рельса, узел 1) BMC — это не съёмная плата `ASMB4-iKVM` (её нет), а встроенный чип `ASPEED` с **выделенным (dedicated) физическим портом**, отдельным от LAN1/LAN2. Подтверждено и по `lspci` (нет отдельного PCI-устройства сетевой карты под BMC, только `ast` VGA/BMC-чип), и по факту — BMC отвечает на собственном IP независимо от портов LAN1/LAN2. **Следствие: бюджет портов в разделе 2 этого документа не сходится** — нужно 2 порта на ASUS-узел (LAN + IPMI), а не 1 совмещённый, как считалось (48 узлов × 2 = 96 портов вместо 48 заложенных, при 96 доступных портах на 2 свитчах суммарно с учётом Supermicro и аплинков бюджет уходит в минус). Требуется пересчитать раздел 2-3 документа целиком либо принять первую фазу с меньшим числом одновременно подключённых ASUS. Источник: `cluster_operations_log.md`, запись 2026-07-03.
- [ ] Проверить на ещё 2-3 узлах (из этой же рельсы и по возможности другой), что находка не уникальна для одного экземпляра — теоретически платы из разных партий/ревизий CERN могут отличаться.
- [ ] Фактическое число исправных RS720QA/RS724QA (может быть меньше 48 узлов).
- [ ] Наличие и тип 10GbE-модулей на обоих HP 3500yl.
- [ ] Есть ли встроенные 10GbE (как у `pve01` — X10DRI-T/Intel X540) на остальных 9 Supermicro — определяет, откуда брать физическую сеть под Ceph (VLAN 60/61, не входит в раскладку этого документа, см. раздел 4.1).
- [ ] Сколько Supermicro реально получат Ceph OSD (3 core или все 10 при схеме "1 JBOD на ноду") — влияет на требуемую ёмкость отдельной 10GbE Ceph-сети.
